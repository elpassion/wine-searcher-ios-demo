import UIKit

extension UIImageView {

    static var cardDetailsWineImageView: UIImageView {
        let imageView = UIImageView(frame: .zero)
        imageView.contentMode = .scaleAspectFit
        return imageView
    }

}
